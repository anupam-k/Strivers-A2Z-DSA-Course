#include <bits/stdc++.h>
using namespace std;

// Take two numbers ad print its sum
int sum(int a, int b)
{
    return (a + b);
}

int main()
{
    int num1, num2;
    cin >> num1 >> num2;
    cout << "Sum: " << sum(num1, num2) << endl;
    return 0;
}